# 风格库 (Styles Library)

此目录用于存放所有提取或手动创建的风格定义文件 (`.xml`)。

## 如何使用
1. **自动提取**: 使用 `svg-style-factory` 技能的 "Image-to-Style" 模式，Agent 会自动将分析结果保存于此。
2. **手动创建**: 你可以参考 XML 模板手动编写新的风格文件。

## 文件命名规范
建议使用 kebab-case 命名法，例如：
- `acid-cyberpunk.xml`
- `tech-minimalist.xml`
- `retro-poster.xml`
